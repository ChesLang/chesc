[Symbol]{
    Space <- " " : "\t",
    NewLine <- "\n",
    LineEnd <- Space* (NewLine : ";"),
}

[Misc]{
    + use Keyword,

    ID <- !Keyword.Reserved ([a-zA-Z_] [a-zA-Z0-9_]*),
}

[Keyword]{
    Reserved <- "data" : "end" : "enum" : "impl" : "macro" : "struct" : "trait",
}
